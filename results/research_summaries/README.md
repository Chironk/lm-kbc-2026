# Historical research summaries

This directory retains the compact `RESULT.md`, `RESULT.json`, and reproduction
records from superseded local experiments. Large generated responses, graphs,
logs, smoke-test outputs, and checkpoints were intentionally removed during the
release cleanup.

These files document historical development findings only. They are not inputs
to the final pipeline. The resumable final test run remains under
`experiments/heterogeneous_agents/runs/final_test_release_candidate_20260809_v2/`.
